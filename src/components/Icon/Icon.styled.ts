import styled from 'styled-components'

export const IconWrapper = styled.a`
  margin: 10px;
  cursor: pointer;

  svg {
    transition: color 0.5s ease;
    color: #fff;
    &:hover {
      color: #e50023;
    }
  }
`
